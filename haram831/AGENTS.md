# Project Instructions

Before making any changes, read:

- `docs/HERMES_MODEL_DEVELOPMENT_PLAN.md`

Important rules:

- Do not modify `baseline/original_baseline.ipynb`.
- Do not start full training or full test inference unless explicitly requested.
- Do not commit or push unless explicitly requested.
- Use small debug subsets for smoke tests.
- After every task, show `git status` and `git diff`.