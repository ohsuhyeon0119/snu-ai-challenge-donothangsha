# 하라는행샤는안하고 — SNU AI Challenge 2026 (프레임 순서 복원)
# 최종 LB 0.92146 파이프라인이 검증된 버전. transformers 5.14의 Qwen3-VL
# M-RoPE / KV-cache 동작에 코드가 의존하므로 아래 버전을 그대로 고정한다.
#
# torch는 CUDA 빌드가 환경마다 달라 여기서 핀하지 않는다. 학습·검증에 쓴
# 정확한 빌드는 torch==2.12.0+cu130 이며, 동등한 CUDA 12.x/13.x 빌드를
# 공식 인덱스에서 먼저 설치한 뒤 아래를 설치할 것. (setup.sh 참고)
transformers==5.14.1
peft==0.19.1
accelerate==1.14.0
bitsandbytes>=0.43
qwen-vl-utils==0.0.14
safetensors>=0.4
pandas>=2.0
numpy>=1.26
pillow>=10.0
gdown>=5.0        # 외부 저장소(Google Drive)에서 가중치 자동 다운로드용
